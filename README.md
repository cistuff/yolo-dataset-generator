# YOLO Dataset Generator

Generate YOLO datasets from a folder of images.

## Features

- Convert folder of images into YOLO dataset (Each image must consist solely of the object you want to classify)
- Generate YOLO-compatible datasets
- Simple and lightweight
